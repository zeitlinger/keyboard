# Keyboard Layout TODOs

## Adaptive Keys

### Reserved physical keys — do not use as adaptive triggers
- `fh` — candidate for chord trigger

### Deferred
- **Same-letter adaptives** (ll, ff, mm, nn, dd, pp, cc, tt): only viable when sacrifice ≈ 0%;
  needs a decision on whether the UX cost of learning is worth it.
- **ss**: no good adaptive — only candidate `s+x→s` is itself a bad stagger motion (sx).
